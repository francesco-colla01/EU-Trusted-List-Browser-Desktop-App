package project.graphics.demo;

//Apparently a useless class, but created only because
//.jar builder needs a target class in which there is the
//main method
public class App {
    public static void main(String[] args) {
        CompleteUI.main(args);
    }
}

